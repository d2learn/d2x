# Task 04 - 校验 workflow 并收尾

## 目标
- 对新增/更新的 workflow 做针对性验证，确保可读、可运行、关键路径正确。

## 子任务
- [ ] 检查 YAML 语法结构。
- [ ] 检查 artifact 命名与下载路径一致性。
- [ ] 检查 macOS 打包逻辑中的路径与命令兼容性。
- [ ] 输出验证证据（命令输出）。

## 完成判定
- 关键验证通过，无明显语法与路径错误。
